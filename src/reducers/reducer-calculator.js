export default function(state =[], action){
    switch(action.type){
        case 'ADD_NUMBERS':
            return '';
        case 'MULTIPLY_NUMBERS':
            return ''
        default:
            return state;
    }
}